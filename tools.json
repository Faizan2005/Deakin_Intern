{
  "echo_tool": {
    "name": "Echo Tool",
    "description": "Repeats the input given by the user.",
    "input_schema": {
      "type": "object",
      "properties": {
        "input": { "type": "string", "description": "The text to echo back." }
      },
      "required": ["input"]
    },
    "output_schema": {
      "type": "object",
      "properties": {
        "output": { "type": "string" }
      }
    }
  },
  "greet_user": {
    "name": "Greet User",
    "description": "Greets the user by name.",
    "input_schema": {
      "type": "object",
      "properties": {
        "name": { "type": "string", "description": "The name of the user to greet." }
      }
    }
  },
  "get_server_status": {
    "name": "Get Server Status",
    "description": "Retrieves the current status of the MCP server.",
    "input_schema": {
      "type": "object",
      "properties": {}
    }
  }
}